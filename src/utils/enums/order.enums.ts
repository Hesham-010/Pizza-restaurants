export enum OrderStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  ONWAY = 'ONWAY',
  DELIVERY = 'delivery',
  CANCELED = 'canceled',
}

export enum ReceiveMethod {
  PICKUP = 'pickup',
  DELIVERY = 'delivery',
}
